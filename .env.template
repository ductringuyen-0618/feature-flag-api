# DigitalOcean API token (Apps: Read & Write)
# Create at: https://cloud.digitalocean.com/account/api/tokens
# Then: cp .env.template .env  &&  fill in the value  &&  doctl auth init --access-token "$DIGITALOCEAN_ACCESS_TOKEN"
DIGITALOCEAN_ACCESS_TOKEN=

# Optional: App Platform defaults (used once we deploy)
# DO_APP_NAME=feature-flag-api
# DO_REGION=nyc
